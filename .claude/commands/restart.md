---
description: Reinicia La Tapeta (mata puerto 3000 + chrome + SingletonLock, relanza server, health-check)
allowed-tools: Bash(bash *), Bash(lsof *), Bash(pkill *), Bash(osascript *), Bash(curl -s *), Bash(open *)
---

Ejecuta `bash ~/Desktop/latapeta/.claude/restart.sh` y repórtame el resultado del health check en una línea. Si el health check no responde tras el arranque, dímelo (puede que WhatsApp esté pidiendo QR o que el puerto siga ocupado).
